# Review Journal

I treated `axiom-store-index-runner` as a project where the smallest useful behavior should still be inspectable.

The local checks classify each case as `ship`, `watch`, or `hold`. That gives the project a small review vocabulary that matches its databases focus without claiming live deployment or external usage.

## Cases

- `baseline`: `index fit`, score 175, lane `ship`
- `stress`: `join width`, score 188, lane `ship`
- `edge`: `constraint risk`, score 156, lane `ship`
- `recovery`: `plan drift`, score 160, lane `ship`
- `stale`: `index fit`, score 212, lane `ship`

## Note

This file is intentionally plain so the fixture remains the source of truth.
